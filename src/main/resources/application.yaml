spring:
    datasource:
      url: jdbc:postgresql://localhost:5432/postgres1
      username: postgres
      password: cs@springfive
      driver-class-name: org.postgresql.Driver
    jpa:
      show-sql: true
      generate-ddl: true
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      hibernate.ddl-auto: update
#    mongodb:
#      database: cms
#      host: localhost
#      port: 27017
